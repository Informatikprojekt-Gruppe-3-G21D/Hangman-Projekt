# Library "random" importieren (ansonsten kann die Funktion "random" nicht vom Computer interpretiert werden)
import random

# Wörterliste (Wörter, die gesucht sein könnten) -> wird in späteren Versionen erweitert
Wörter = ["bananen"]

# Zufälliges Wort aus der Liste wird ausgesucht und zur Kontrolle gedruckt
random.choice(Wörter) = GesuchtesWort
print(GesuchtesWort)

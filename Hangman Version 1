# Library "random" importieren (sonst kann die Funktion "random" nicht vom Computer interpretiert werden)
import random
 
# Wörterliste definieren (in späteren Versionen erweitert)
Wörter = ["banane"]
 
# Zufälliges Wort aus der Liste wird ausgesucht und zur Kontrolle gedruckt
GesuchtesWort = random.choice(Wörter) 
print(GesuchtesWort)


# MIT License: Copyright (c) [2022] [Larissa Carver, Michelle Koch, Sophie Germann]
